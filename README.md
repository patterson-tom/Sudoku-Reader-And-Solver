# Sudoku-Reader-And-Solver
Uses OpenCV and pytesseract to read an image of a sudoku grid into memory, and then solve the sudoku

## Requirements
* python OpenCV livrary installed
* pyTesseract installed
